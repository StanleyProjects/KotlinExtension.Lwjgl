package sp.kx.lwjgl.entity.input

enum class Key {
    ESCAPE,
    Q, W, E, R, T, Y, U, I, O, P,
    A, S, D, F, G, H, J, K, L,
    Z, X, C, V, B, N, M
}

enum class KeyState {
    RELEASE,
    PRESS
}
